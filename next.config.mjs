/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "promptsgenii.com",
      "cdn.midjourney.com",
      "tailwindui.com",
      "ovs927.p3cdn1.secureserver.net",
    ],
  },
};

export default nextConfig;
